# URL du backend - décommentez la ligne selon votre configuration

# Si le proxy React fonctionne (normal), laissez cette variable vide
REACT_APP_API_URL=

# Si le proxy ne fonctionne pas, utilisez l'URL directe du backend :
# REACT_APP_API_URL=http://localhost:5000
